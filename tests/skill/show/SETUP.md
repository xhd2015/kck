# Scenario

**Feature**: `kck skill --show` root and topic content

```
Caller -> MainWith(["skill","--show",…]) -> markdown on stdout
```

## Steps

1. Leaf sets `--show` args (optional topic path).
